{
  "name": "tests",
  "version": "0.0.1",
  "description": "Simple pub-sub message broker",
  "main": "",
  "type": "module",
  "scripts": {
    "build": "node buildWorker.cjs",
    "test": "pnpm link --global browser-message-broker && npm run build && wds",
    "test:w": "wtr --watch"
  },
  "license": "MIT",
  "private": true,
  "dependencies": {
    "lit": "3.1.4",
    "browser-message-broker": "latest"
  },
  "devDependencies": {
    "@playwright/test": "1.42.1",
    "@web/dev-server": "0.4.3",
    "@web/dev-server-esbuild": "1.0.2",
    "eslint": "8.57.0",
    "typescript": "5.4.2",
    "esbuild": "0.20.1"
  }
}
